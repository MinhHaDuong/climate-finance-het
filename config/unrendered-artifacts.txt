# Shared artifacts no deliverable renders, and why that is the right answer.
#
# tests/test_deliverable_artifacts.py resolves the {{< include >}} graph from all
# eleven deliverable .qmd roots and fails on any tracked file under
# deliverables/_shared/{_includes,tables} the graph does not reach. Every entry
# below is an accepted exception; anything else is a defect.
#
# Format: <repo-relative path> — <reason>
# The reason is load-bearing, not decoration: a redundancy check fails the suite
# when an entry becomes reachable, so this file cannot rot into a mute skip-list.


deliverables/_shared/tables/tab_retrieval_protocol.md — deposited in the Zenodo package (build_datapaper_archive.sh copies it to data/products/), not a paper float; data-paper.qmd §2 points readers to it by name for the full per-source protocol and institutional-reports seed list (ticket 0329)

deliverables/_shared/tables/tab_venues_fr_caption.md — build input, not a float: manuscript.mk's $(GIDE_VENUES_FR) rule concatenates it onto tab_venues.md to make the French-caption variant that manuscript-Gide.qmd includes

# Ticket 0290 settled the seventeen top-level _includes/ leftovers of the
# technical report's rewrite and split (0226/0240): fourteen deleted, two moved
# to conception/ as research notes, and prior-mappings.md kept below. 0359 had
# parked all seventeen here so a reader and the guard could both see them; the
# entry that remains is the only one that is still an artifact awaiting a
# consumer. Delete it when the data paper composes it.
deliverables/_shared/_includes/prior-mappings.md — staged for the data paper by ticket 0289, not yet included by it; the data paper's revision draft carries the overlap prose, so the include is retained for the composition pass rather than deleted
