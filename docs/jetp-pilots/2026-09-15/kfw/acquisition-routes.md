# Frozen acquisition routes — 0738

The 517 eligible sample candidates and four excluded mandatory challenges are
frozen in selection.csv. Twelve round-robin ranks are selected without outcome,
status or speed filtering; no replacement is allowed. Generic document links
are leads, not local documentary coverage. Raw XML is local evidence for all.

Maximum 20 external units, including failed requests and independent verification.
Forge preflight is workflow infrastructure, not evidence acquisition. Reserve
units 17–20 for verification; unused allocations carry forward.

1. Semantics, up to eight: retrieve the dated 2020 KfW disclosure statement and
   current transparency page as original HTML. Inspect inclusion/retention and
   project-database routes. Inspect BMZ transparency definitions. For the failed
   FAQ, use a named changed route: search the official domain for indexed FAQ
   text about annual/quarterly transaction dates; do not re-open the JS shell.
2. Historical roster and case evidence, up to eight: targeted official-domain
   queries for a complete calendar-2018 signed-contract roster for Morocco and
   India; preserve every disclosed roster before following contracts. A current
   search result is not a historical denominator. Then inspect mandatory IDs
   first, including changed raw-HTTP routes for the two previously blocked KfW
   project-detail URLs. Older challenges have generic links: use exact BMZ IDs
   in official-domain queries. Continue with selected sample rank order.
3. Reserve up to four: review original bytes locally first. Any new query/open
   counts against the same cap; stop on decisive missing-evidence disposition,
   named-route exhaustion or 20 units. Record unattempted sample cases.

Identity acceptance needs an explicit BMZ/IATI number or an unambiguous official
cross-reference to that number. Titles/budgets alone are insufficient. Contract
signature, approval, physical progress, payment and JETP attribution stay separate.
The historical denominator remains unknown unless a complete dated roster or
independently validated inclusion/retention history establishes it.

Protocol deviation disclosed before retrieval: the first 35 XML lines were
previewed before directory-MD5 verification. Both entire bundles subsequently
matched their specified hashes before profiling; no selection used that preview.
Country/Region duplicate bytes exist as one archived object; endpoint equality
is historical evidence in round3/profile.json, not two independent snapshots.
