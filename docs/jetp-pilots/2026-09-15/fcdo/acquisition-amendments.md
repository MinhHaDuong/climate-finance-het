# Acquisition amendments

After unit 16: three unused retention units moved to sample ranks 6–8, as the
frozen route permits. Selection and the reserved four units are unchanged.

After unit 17: exact HTTP Intervention Summary link returned 301. The acquisition
client deliberately did not follow redirects automatically, so this is one failed
unit. Try the same issuer/path on HTTPS as an explicitly logged route variant,
then its related completion review (same programme, exact payload document ID).
No inference of document absence from redirection. Stop after these routes or cap.
