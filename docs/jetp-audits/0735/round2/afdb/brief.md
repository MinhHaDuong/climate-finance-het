# AfDB scout adaptation — frozen before search

2026-09-14. Parent round-2 brief frozen at 8c712432 applies. Scope: AfDB official
all-sector operation catalogue, then Senegal/South Africa and Kenya/Morocco/Ghana
operation records. Countries are acquisition strata, not identified controls.

Routes: official AfDB project portal/catalogue first; linked appraisal,
implementation-progress and completion reports; country indicative programmes;
AfDB open-data/IATI and archival documentation. Seek one energy and at least two
non-energy sectors, including at least two non-JETP-country examples. Select on
availability of clear identifiers and source fields, never observed speed.

Identity acceptance: an explicit AfDB operation ID joins a project; loan-level
dates require its loan/grant ID and scope. Additional financing is distinct.
A same-operation record explicitly labelling two actual lifecycle dates passes
the first milestone check. Planned dates, page updates, approval and signature
are not interchangeable; payments need explicit semantics. A current list does
not establish historical entry/removal or proposed/cancelled coverage.

Budget: 30 external query/URL units or 60 minutes, whichever first. Each query,
open URL and failed URL counts separately; local reads/writes do not count.
Stop when evidence is sufficient or limit reached; no hidden second round.

Return: report, event evidence CSV, source metadata and acquisition log in this
subdirectory only. Original locators and retrieval/publication dates required;
hashes only for actual locally archived bytes. No Git/forge/shared ledger edits,
institutional messages, construction claims, effect estimation or design choice.
Parent verifies and integrates. Separate unknown, unsearched and failed routes.
