# ED-03 — variables table for climate_finance_corpus.csv

Response-letter bullet (feeds ticket 0283; letter-voice: first person
singular).

> **ED-03** (variables table). Done. Section 3 now includes Table
> @tbl-variables, describing every variable of `climate_finance_corpus.csv`:
> name, type, description, and the pipeline step that produces it. The table
> covers the four provenance columns added with the curated key-documents
> layer (`from_unfccc`, `from_oecd`, `abstract_provenance`,
> `keywords_provenance`). It is generated by the pipeline from the deposit's
> declared column contract, which the export script checks when writing the
> CSV, so the published table cannot drift from the deposited file. The
> paragraph describing the file was shortened accordingly, replacing the
> inline column enumeration with a reference to the table.

Implementation record: ticket 0279 — contract in
`scripts/_deposit_variables.py`, renderer `scripts/figures/export_variables_table.py`,
output `deliverables/_shared/tables/tab_variables.md`.
