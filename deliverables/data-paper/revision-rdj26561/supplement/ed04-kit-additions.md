# Files to add to the ed04 kit's data/products/ (cut-plan v2, ticket-ref 0274)

The cut pass moved detail out of data-paper.qmd into the Zenodo package. When
rebuilding the ed04 kit (ticket 0280), add to `data/products/`:

- `tab_lit_confirmations.csv` and `tab_semantic_robustness.csv` (shared tables
  dir; generated by `scripts/analysis/compute_lit_confirmations.py` and
  `compute_semantic_robustness.py`) — the six literature-confirmation
  statistics the paper's §1 now cites by pointer only (W1).
- `codebook.md` gains the "Citation coverage over time" note
  (`codebook-citation-coverage-note.md` in this directory) (W3).
- The package README gains the "Recovering OA links" section
  (`readme-recovering-oa-links.md` in this directory) (W7).
- Notes accompanying `tab_dedup_error_estimates.csv`
  (`dedup-audit-notes.md` in this directory) (condensation #1).
- The package README gains the raw-pool storage / overlap-validation detail
  (`readme-raw-pool-note.md` in this directory) (condensation #6), if that
  condensation was applied.
