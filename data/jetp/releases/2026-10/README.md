# JETP observatory 2026-10 — fact-unchanged rehearsal release

`jetp-observatory-2026-10.zip` is a second immutable, offline rehearsal package.
It reuses the same frozen source facts and input commit as `2026-09`; its
descriptor names `2026-09` as `rehearsal_of` and records
`no_scientific_change: true`. The distinct edition proves package history and
recovery mechanics, not a new observation, correction, or publication.

The archive contains the static site, four country downloads, provenance
sidecar, dictionary, coverage report and reuse terms. It excludes raw source
documents and restores without Git, DVC, a database, or network access.

This package is **prepared**, not externally deposited or hosted. It preserves
the earlier `2026-09` archive as a separate immutable package.
