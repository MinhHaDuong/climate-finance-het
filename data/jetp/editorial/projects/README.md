# Projects

Use the existing project_id as the filename. Do not create a new project by adding a Markdown file; reconcile its identity in projects.csv first.

Copy the corresponding [template](../templates/). No reviewed dossiers have
been added by this storage-planning change.
