%erg 0.1
Title: Materialize pinned CRS inputs before the JETP full gate in fresh worktrees
Created: 2026-09-24
Author: Codex
Closed: Pinned CRS pull, reference replay, and full-gate preflight verified in a fresh worktree at 448c0f45

--- log ---
2026-09-24T20:22Z Codex created from full `make check` on 0876 draft head 5a1f6da4: reference-chain integration test fails at its explicit CRS-directory assertion; 5 failed, 3285 passed, 62 skipped overall.
2026-09-25T06:03Z Codex note pinned DVC pull and reference replay passed in fresh worktree at 448c0f45; full gate now reserved for padme/main with 16 workers.

2026-09-25T06:04Z HDMX-coding-agent closed — Pinned CRS pull, reference replay, and full-gate preflight verified in a fresh worktree at 448c0f45
--- body ---
## Failure

`tests/test_jetp_courbe_reference.py::test_chaine_portee_reproduit_la_reference`
fails at line 68 because `data/jetp/crs` is absent in a fresh 0876 worktree.
The test intentionally fails rather than skipping: it needs the archived
80 `*_micro.csv.gz` files to replay the pinned CRS calculation and compare
the output cell by cell with
`conception/jetp/papier-court-mesure/courbe-reference-decaissement-2026-09-08.csv`.
The test never reached the comparison, so the result establishes missing
test data, not a successful or failed numerical reproduction.

`make jetp-data` only checks out JETP documents and a VNM release
(`Makefile:234-237`); it does not fetch CRS. The dedicated
`jetp-crs-data` target at `Makefile:150-151` runs `dvc pull data/jetp/crs`.
This full-gate prerequisite was not run in the worktree. The four separate
country-scan failures in the same full suite are tracked in [[0940]].

## Work

- Verify that the pinned CRS DVC object is available from the configured
  remote and that `make jetp-crs-data` materializes exactly 80 archived
  `*_micro.csv.gz` inputs in a fresh worktree. If retrieval fails, capture
  the concrete remote/cache error and restore the missing pinned object.
- Make the full-gate setup state the CRS prerequisite before starting the
  14-minute suite, or materialize it as part of the documented full-gate
  workflow. Keep the test's fail-when-absent behavior.
- Rerun the named integration test and compare every output row with the
  versioned reference; retain the pinned 98.56% and 37.35% central figures.

## Exit criteria

- [x] `make jetp-crs-data` succeeds in a fresh worktree and exactly 80
  `*_micro.csv.gz` files are present under `data/jetp/crs`.
- [x] The named reference-chain integration test passes and its output is
  byte/cell equivalent to the versioned reference as asserted by the test.
- [x] The full-gate workflow catches missing CRS inputs before the main test
  suite, with an actionable command; it never silently skips this test.

## Verification

- Commit `448c0f45` uses `uv` for the DVC pull (`Makefile:150-151`). A fresh
  detached worktree at that commit had no `data/jetp/crs` before the pull;
  `make jetp-crs-data` succeeded and 80 matching files were counted afterward.
- In that fresh worktree, `pytest tests/test_jetp_courbe_reference.py
  tests/test_full_gate_preflight.py -q` passed (5 tests). The named replay
  compares every output row with the versioned reference and checks the
  98.56% and 37.35% central figures (`tests/test_jetp_courbe_reference.py:68-102`).
- The preflight check and its 0/79/80-file tests are at
  `scripts/qa_full_gate_preflight.py:76-83` and
  `tests/test_full_gate_preflight.py:54-84` in `448c0f45`. `make lint` passed
  (339 passed, 11 skipped). The local `make check-fast` reached 2344 passes;
  its two socket-restricted setup errors passed when rerun individually with
  socket access. The local full gate was stopped following the current
  padme/main-only workflow; no full-gate success is claimed here.
