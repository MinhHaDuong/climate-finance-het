%erg 0.1
Title: Scission des identités : référents, projets, actifs, accords, parties, périmètres
Created: 2026-09-22
Author: HDMX-coding-agent
Closed: PR #1496 merged at 8a91c1fd; independent postmerge review green (134 focused tests passed, no blocking findings); exit criteria and invariants verified

--- log ---
2026-09-22T17:30Z HDMX-coding-agent created
2026-09-23T10:30Z HDMX-coding-agent note revue du train (2026-09-23) : `relations` ajoutée aux critères de sortie ([[0833]] en dépend) ; le palier 2 tourne ici une seule fois

2026-09-23T09:17Z HDMX-coding-agent note gaze/1457: 0871's LEGACY_FILES skips data/jetp/projects.csv by name in the ledger header check so the pre-migration file doesn't fail as 'projects' table header; remove that skip entry (scripts/jetp/_ledger_headers.py) once this ticket's split retires the file
2026-09-23T14:59Z HDMX-coding-agent note décision de l'auteur (2026-09-23, interactive) : contrôle d'autorité des organisations. La table publishers est fondue dans parties (un publisher est une partie dans un rôle de publication) ; nouvelle table party-names (une ligne par forme imprimée, form_type, langue, justification, une seule forme preferred en vigueur). L'étape 4 de ce ticket part des parties et formes déjà frappées par 0872 (PR #1461) : palier 1 par external-ids (IATI org, ROR, LEI, Wikidata), variantes de casse fusionnées à la frappe, acronymes/traductions/anciens noms en candidats de palier 2. Action 4 réécrite en conséquence.
2026-09-23T17:04Z HDMX-coding-agent note 0873 opened data/jetp/routes.csv with 1 907 rows of kind line (plan_project_id and VNM inventory_id); the 404 projects.csv identifiers of action 5 are appended to it, not written over it — no key overlaps (checked).
2026-09-24T14:05Z HDMX-coding-agent note blocker 0874 closed — Blocked-by removed.
2026-09-24T15:09Z Codex note décision de l'auteur dans cette session : après PR #1492, les adresses de l'ancien aperçu n'ont jamais été publiées ; ne pas ajouter les 404 anciens identifiants à routes et ne pas réintroduire leur redirection navigateur. Les routes de lignes M1a déjà présentes servent seulement à retrouver les identifiants de ligne imprimés dans l'export.
2026-09-24T16:01Z Codex note vérification du contrat DDL : `line-referents` justifie projets, actifs et accords ; les parties ont leur base dans `party-names`, les périmètres dans la table d'ontologie et leurs observations chiffrées suivent en [[0877]]. Le critère de vérification ci-dessous est précisé en ce sens. Deux libellés IDN regroupent chacun deux lignes de subvention distinctes et restent en attente plutôt que d'être promus en accord unique.
2026-09-24T16:04Z Codex note preuves de sortie sur PR #1496 : `test_committed_split_is_replayable_and_every_identity_has_a_line` (tables et validation SQL), `test_register_allocations_slots_and_component_links` (404 dispositions, routes figées par SHA-256, 21 places, 11 composants, dix candidats non acceptés), `test_ten_cases_cover_the_five_dispositions` (dix cas réels dont un en attente), rapport `docs/jetp-identity-split-0875.md` (comptes et vingt attentes). `make check-fast` : 2316 passés, 10 ignorés ; `make lint` : 339 passés, 11 ignorés. Les quatre vues pays restent identiques octet pour octet à `origin/main`.
2026-09-24T18:45Z HDMX-coding-agent closed — PR #1496 merged at 8a91c1fd; independent postmerge review green (134 focused tests passed, no blocking findings); exit criteria and invariants verified
--- body ---
## Context

Étape 4 de la migration (`docs/jetp-ledger-migration.md`), décisions 1 et 6. `projects.csv` (404 lignes)
mélange quatre genres : 257 allocations de bailleur du registre ZAF (166
noms distincts), 21 places comptées VNM, 43 soumissions SEN, 4 en-têtes de
programme, et le reste des projets. Chaque ligne reçoit une disposition
et une identité n'est frappée que par une ligne `line-referents` avec sa
base (contrat de stockage § 4, palier 1 : identifiant exact ; palier 2 : libellé normalisé, en
candidats revus à la main). La table `parties` existe déjà : [[0872]] l'a
ouverte avec les publishers, sous contrôle d'autorité (décision de l'auteur du
2026-09-23, ontologie décision 12 ; contrat de stockage § 4, organisations).
Ce ticket y ajoute les rôles bailleur et canal d'abord, depuis les libellés
du journal financier et les 14 préfixes du registre. Les anciens identifiants
de l'aperçu non publié restent dans le rapport de disposition, pas dans `routes`.

## Relevant files

- `data/jetp/migration/0875-projects-legacy.csv` — l'entrée figée à scinder ; `data/jetp/projects.csv` — la sortie
- `scripts/jetp/build_reconciliation.py` — paliers 1 et 2, enregistrement des décisions
- `deliverables/jetp-observatory/data/<CODE>.json` — vues pays à comparer au bit près

## Actions

1. Disposition par ligne de `projects.csv` depuis `verification_status`
   (official_register → agreement, official_count_slot → observation de
   périmètre, official_plan → ligne submission, official_programme →
   classification programme), revue à la main des 9 restants.
2. Palier 1 : frapper les 257 accords (`modality` depuis la ligne,
   `sector` par crosswalk de la fenêtre), les projets par nom exact
   confirmé, les 3 projets VNM nommés, les actifs nommés par les lignes de
   plan IDN (Cirebon-1, Pelabuhan Ratu comme actifs sans projet).
3. Palier 2 en candidats : libellés normalisés dans un pays et un groupe
   technologique ; aucun `accepted` sans revue.
4. `parties` et `party-names`, en partant des parties déjà frappées par
   [[0872]] :
   - palier 1 : un identifiant externe dans `external-ids` (identifiant
     d'organisation IATI, ROR, LEI, Wikidata) ; même identifiant, même partie ;
   - à la frappe, les variantes de casse, de diacritiques ou d'espaces d'un
     libellé bailleur ou canal sont une seule partie avec plusieurs lignes
     `party-names` (`spelling_or_case_variant`), jamais deux parties et un
     `same_as` ; un libellé qui est la forme d'une partie existante (AFD,
     World Bank…) s'y rattache au lieu d'en frapper une nouvelle ;
   - palier 2 en candidats : acronymes, traductions, anciens noms (EVN /
     Vietnam Electricity / Tập đoàn Điện lực Việt Nam, PLN / Perusahaan
     Listrik Negara) en `same_as` candidats entre parties, revus à la main ;
   - chaque forme lue est une ligne `party-names` justifiée par sa ligne ;
     une seule forme `preferred` en vigueur par partie ;
   - `party_in` un rôle par ligne ; `role_in` pour les entités de mise en
     œuvre du registre.
5. Les 404 anciens identifiants ont chacun une disposition dans le rapport ;
   aucune route publique ancienne n'est créée (décision de l'auteur du 2026-09-24).
6. Les 11 `project_page_component` deviennent `component_of`.

## Test

- `tests/test_jetp_identity_split.py` : depuis une fixture de dix lignes
  de `projects.csv` couvrant les cinq dispositions, la scission frappe les
  identités attendues et n'en frappe aucune sans référent.

## Verification

- [x] Aucun projet, actif ou accord sans ligne `line-referents` acceptée ; chaque partie a sa forme justifiée
- [x] `COUNT(projects)` nomme son unité ; les 21 places ne sont plus des lignes de registre
- [x] Les 404 lignes ont une disposition ; aucune nouvelle route des anciens IDs n'est publiée

## Invariants

- Les recettes ZAF et VNM de [[0834]] restent vertes pour les adresses publiées.
- La ligne M1a de 0725 et 0715 cochée par l'auteur n'est pas retouchée.

## Exit criteria

- [x] Cinq tables d'identité, `line-referents`, `relations` (dont les 11 `component_of`), `parties` validées par [[0871]]
- [x] Les candidats de palier 2 sont écrits pour les lignes des quatre documents M1a ; [[0833]] les revoit sans relancer le palier 2
- [x] Rapport de scission : comptes par disposition, candidats en attente
- [x] `make check-fast` et `make lint` verts
