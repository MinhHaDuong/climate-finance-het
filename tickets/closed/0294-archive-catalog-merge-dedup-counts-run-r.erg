%erg 0.1
Title: Archive catalog_merge dedup-counts run report via DVC (0284 deferred step)
Created: 2026-07-22
Author: HDMX-coding-agent
Closed: autoclosed — PR #1116

--- log ---
2026-07-22T12:23Z HDMX-coding-agent created
2026-07-24T12:30Z HDMX-coding-agent note archived data/catalogs/run_reports/catalog_merge__20260724T112050Z.json via dvc commit+push; run_reports.dvc bumped (md5 2e7d3f3e, 120 files). Counts differ from the ticket's reference numbers because the author expanded the seeds today (0288 lane, PR #1114) before this rerun: records_total=44152 (ref 43906), doi_duplicates_removed=833 (unchanged), title_year_duplicates_removed=159 (ref 154), dropped_empty_title=3 (unchanged), records_unified=43157 (ref 42916). Procedures unchanged — the deltas are corpus-expansion effects, not procedure drift. Note logged to 0274 tracker instead of closed 0276 archive.
2026-07-24T16:00Z HDMX-coding-agent note superseded by two later rebuilds: (a) full 35-seed OECD config + ECDPM grey entry (they were missing from the 112050Z pass — primary main was 26 commits stale), (b) forced GROBID re-parse with PYTHONPATH set. Current archived report catalog_merge__20260724T132552Z.json: records_total=44174, records_unified=43179 (doi dups 833, title-year 159, empty-title 3 — all unchanged). Merge procedures still unchanged; the +22 unified vs 112050Z is the extra OECD/ECDPM seeds. Refined dropped 38,166 -> 33,344 between passes: DIAGNOSED as a filter-stage defect in the 112050Z pass, not a data change — its Flag 6 (semantic relevance, sentence-transformers) logged "Flag 6 skipped: torch/sentence-transformers not installed -> no candidates scored", flagging 0 llm_irrelevant; the 132552Z pass had torch on PYTHONPATH, ran Flag 6, flagged 5,840 llm_irrelevant. The 33,344 corpus is the correct one; PR #1116's 38,166 was inflated by a silently-skipped filter. run_reports.dvc re-bumped after dvc commit+push of the 132552Z-pass reports.

2026-07-24T20:12Z HDMX-coding-agent closed — autoclosed — PR #1116
--- body ---
## Context

Ticket 0284 (PR #1082) instrumented `scripts/harvest/catalog_merge.py` to emit
per-procedure dedup counts via `save_run_report` (referee remark R1-12). The
counts were reproduced offline and byte-verified (output md5 identical to the
`dvc.lock` archived hash), but `dvc` was absent from the shared env — which
cannot be synced mid-raid — so the run-report JSON was never archived: no
`catalog_merge__*.json` exists on disk, and `run_reports.dvc` / `dvc.lock`
were not bumped. This ticket carries 0284's deferred step 4 so the parent
could close (follow-up-lets-parent-close convention).

Reference numbers (recorded in 0276's log and PR #1082 body):
records_total=43906, doi_duplicates_removed=833,
title_year_duplicates_removed=154, dropped_empty_title=3,
records_unified=42916.

## Actions

1. On a dvc-capable environment (padme or after a safe `uv sync`), run
   `make corpus-discover` (never bare `dvc repro`).
2. Confirm `unified_works.csv` md5 unchanged
   (4b3c9c4bf0a54be5988017921350980f) and the emitted
   `data/catalogs/run_reports/catalog_merge__<run_id>.json` carries the
   reference numbers above.
3. Commit `run_reports.dvc` pointer + `dvc.lock` stage-hash bump; push DVC
   cache per the usual `make corpus-sync` flow.
4. Note the archived artifact path in 0276's log (0276 cites the counts).

## Invariants

- No hand-curated numbers: the archived JSON must come from the rerun, not
  be written by hand.
- Never `uv sync` the shared env while sibling agents are mid-run.

## Exit criteria

- [ ] `catalog_merge__*.json` archived under DVC-tracked run_reports
- [ ] Counts in the archived JSON match the reference numbers above
