%erg 0.1
Title: Data paper: corpus CSV column layout — restructure or document, author decision
Created: 2026-07-20
Author: HDMX-coding-agent
Label: needs-human

--- log ---
2026-07-20T10:00Z HDMX-coding-agent created split from 0279
2026-07-22T11:00Z HDMX-coding-agent note author deferred the decision (session 2026-07-22, batched decision round) — stays needs-human; blocks neither 0288 nor 0279 (variables table lands either way), but 0280's deposit should wait for it to avoid a double Zenodo version bump

2026-07-23T14:00Z haduong decision AUTHOR DECISION: do NOT restructure the CSV. The file is formally correct (pandas-loadable, rectangular, 26 columns, no duplicates) and already organised in 4 logical groups (Record identity, Bibliographic metadata, Provenance flags, Curation metadata); R1-19 is a viewer-ergonomics concern. Answer: a formal data dictionary — column, type, allowed values, missingness measured on the real data — extending the 0279 contract; codebook ships in the Zenodo package (coordinated with 0280, deposit not performed here); rationale paragraph drafted for the response letter (feeds 0283).
2026-07-23T14:05Z HDMX-coding-agent note implemented: contract extended (group, allowed_values, compute_missingness, render_codebook in scripts/_deposit_variables.py), scripts/figures/export_codebook.py, tab_variables.md regenerated with group column, codebook.md generated from real data (42916 rows), r1-19-csv-structure.md response bullet, 0280 log noted for package inclusion

--- body ---
## Context

Split from 0279 (RDJ-26561 R&R, tracker 0274, remark R1-19): the referee
finds `climate_finance_corpus.csv` — one main-information column followed by
unstructured columns — unpleasant to explore manually. Restructuring changes
the shipped data product (column order/shape of a published Zenodo artifact
and every downstream consumer), so it is an author decision, hence
needs-human. 0279 delivers the variables table either way; this ticket
settles whether the CSV itself changes.

## Relevant files

- `climate_finance_corpus.csv` producing script (Phase-1 export; locate it)
- `scripts/schemas.py` — schema to update if columns change
- Downstream consumers of the CSV (grep before deciding blast radius)
- Ticket 0280 — Zenodo restructure; a new CSV shape must ship in the same
  new deposit version

## Actions

1. Enumerate consumers and the blast radius of a column reorder/rename.
2. Author decides: restructure (then implement, update schema + consumers +
   tests, coordinate with 0280's deposit) or keep layout and answer R1-19
   via the variables table + codebook, with the rationale in the response
   letter.

## Test

- If restructured: schema validation + downstream tests green;
  byte/content-level check of the regenerated CSV against expectations.

## Verification

- [x] Decision recorded in the log with rationale (2026-07-23 author decision)

## Invariants

- Concept DOI stability on Zenodo; downstream pipeline unbroken.

## Exit criteria

- [x] Decision made; either restructured CSV landed and shipped via 0280, or
      documented-layout rationale drafted for 0283
