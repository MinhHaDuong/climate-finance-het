%erg 0.1
Title: Corpus v2: curated international-institution key-documents layer (UNFCCC + OECD DAC)
Created: 2026-07-20
Author: HDMX-coding-agent

--- log ---
2026-07-20T11:00Z HDMX-coding-agent created author approved expansion within the UN-system frame (session 2026-07-20)
2026-07-20T12:00Z HDMX-coding-agent note broadened to include OECD DAC sub-list after corpus probe (author asked "harvest OECD too"); wholesale OECD harvest rejected as redundant
2026-07-22T09:00Z HDMX-coding-agent note author angle: manuscript-bibliography coverage probe run — empirical basis for the inclusion rule; see body § Empirical basis
2026-07-22T10:00Z HDMX-coding-agent note author decided (session 2026-07-22): party/bloc submissions AND negotiation-record commentary INCLUDED in the layer; carbon markets and ESG are deliberate corpus-scope exclusions — MacKenzie 2009 and Golka 2024 verified out-of-scope on the documents (not gaps)
2026-07-22T11:00Z HDMX-coding-agent note author VALIDATED the amended inclusion rule and both sub-lists (class level). Framing constraint: these are new SOURCES added to the data pipeline — reproducible Phase-1 harvesters/importers with provenance and tests — NOT a one-shot harvest
2026-07-22T23:55Z HDMX-coding-agent note author decisions on review consider-tier (session 2026-07-22): keywords never faked from short_title — first-page keywords block extracted, else deterministic lexicon generation, disclosed via keywords_provenance; filename-stem collisions rejected at seed validation; harvest_entry's load_seed contract documented
2026-07-22T23:00Z HDMX-coding-agent note BUILD phase implemented on branch t288-corpus-v2-layer: catalog_keydocs harvester, starter seed lists (47 UNFCCC + 14 OECD), merge/protection/disclosure wiring, dvc stages. Document-level enumeration completion + padme harvest split to child ticket 0304

2026-07-27T13:06Z HDMX-coding-agent note VERIFICATION PASS against frozen corpus v2 (2026-07-27). Exit 1 MET: layer in the v2 corpus and counts refreshed — tab_corpus_sources.md (generated) reads UNFCCC 232 raw / 230 refined / 225 unique and OECD DAC 35 / 33 / 33, matching a direct query of refined_works.csv (from_unfccc=230, from_oecd=33, 263 total). Exit 3 MET: response-letter.md:81-101 answers R1-06 with R1-08, carries 0281's national-institutions boundary rationale and the 230/33 counts. Exit 2 WAS NOT MET and is fixed on this branch: the bias section disclosed the citation-network exclusion only generically (no-DOI works undercounted, 'concentrates in early and grey-literature works') and never named the layer; @tbl-quality's %Refs 0%/6% was its only explicit trace. Added one sentence to section 2.3, numbers referenced via @tbl-quality rather than hand-typed (data-paper-vars.yml is gitignored and has no keydocs var).
2026-07-27T13:06Z HDMX-coding-agent note VERIFICATION of the two open boxes. Provenance flags: from_unfccc / from_oecd / abstract_provenance all correct (245 reconstructed, 3 original, 15 missing on the layer), BUT keywords_provenance is empty for all 33,344 refined rows while 242 of 263 key documents carry lexicon-generated keywords. Cause isolated to one line: catalog_keydocs.main() built its DataFrame with columns=WORKS_COLUMNS + ['abstract_provenance'], so build_record's keywords_provenance was selected away between the record and the CSV — unfccc_works.csv and oecd_works.csv have no such column at all. The author rule of 2026-07-22 (keywords never faked, disclosed via keywords_provenance) is half-shipped: the honest derivation runs, the disclosure does not. codebook.md is not wrong — it truthfully reports the column 100% missing, so the deposit documents a column it never fills. Fixed on this branch, plus the test that catches it: the 41 existing tests passed because they assert on build_record's dict and on a synthetic DataFrame that already has the column, never on the emitted CSV. Repopulating the data needs a keydocs re-harvest (warm pool, offline) + catalog_merge + corpus_filter — additive metadata, no count moves — which is a Phase-1 rerun on a frozen corpus, so it is the author's call; natural home is 0344's Phase-2 rebuild.
2026-07-27T13:07Z HDMX-coding-agent note VERIFICATION of in_v1 reproducibility (config/v1_identifiers.txt.gz: 21,634 DOIs + 8,241 sids = 29,875). Recovered in refined v2: 21,607 DOIs + 7,707 sids = 29,314 identifiers present, flagged on 28,976 rows (fewer than 29,314 because dedup collapses DOI+sid pairs onto one v2 row). Absent: 561 identifiers (1.9%) — 503 of them still in unified_works.csv, so removed by v2's stricter six-flag filter (expected, Flag 6 ran), and 58 (19 DOIs + 39 sids, 0.2%) absent from the v2 harvest entirely. Reading: the v1 subset is reproducible to 98.1% of its identifiers, with a quantified 58-work harvest-level gap. Dedup claims in the Test section verified: 5 keydocs merged onto existing grey seeds (Biennial Assessments 4th/5th, three SCF summaries) rather than duplicating them, and of 372 10.1787 rows in refined only 4 come from the OECD layer — no wholesale re-add. One exact title collision (Report of the Green Climate Fund to the Conference of the Parties: unfccc FCCC/CP/2015/3 vs a scispace row with no year) is the empty-year title key, ticket 0299's territory, not a layer defect.
2026-07-27T13:08Z HDMX-coding-agent note residual filed as 0347 (needs-human): repopulate keywords_provenance in the corpus. Recommended option is to fold the offline re-harvest into 0344's Phase-2 rebuild. All three exit criteria are now met, so this ticket closes on merge of its PR — which unblocks 0283, the response letter.
--- body ---
## Context

RDJ-26561 R&R (tracker 0274), remark R1-06. Instead of only defending the
grey-literature boundary (ticket 0281), the author decided to extend it
within the existing international-institution frame: a curated layer of
key documents in two sub-lists.

**UNFCCC sub-list.** COP/CMP/CMA decisions bearing on finance (Copenhagen
$100bn, Paris Article 9 package, NCQG), Standing Committee on Finance
reports — especially the Biennial Assessments, which define climate finance
accounting — GCF/GEF/Adaptation Fund reports to COP, and pre-1995 INC
negotiation documents. Order of magnitude 150–300 documents.

**OECD DAC sub-list** (added 2026-07-20 after a corpus probe). The refined
corpus already holds 310 OECD-DOI (10.1787) works via OpenAlex, including
the flagship accounting series (USD 100 Billion Goal 2015; Provided and
Mobilised aggregate trends; DAC blended-finance guidance) and the Oxfam
Shadow Report counter-literature — so NO wholesale OECD harvest. The gaps
are the founding documents outside the DOI system: the DAC Statistical
Reporting Directives establishing the Rio markers (1998 onwards — zero
source directives in corpus, one academic paper about them), Development
Co-operation Reports (zero hits), and early DAC/WP-STAT/ENVIRONET documents
(corpus OECD-DOI coverage starts at 2000). Order of magnitude a few dozen
documents, from the OECD OLIS/One archive.

Known data properties (assessed 2026-07-20, verify at harvest):
- **No native abstracts** — derive abstract-equivalents (executive summaries,
  decision preambles) via the existing abstract-reconstruction pathway,
  flagged as reconstructed.
- **Fulltext**: open PDFs from the UNFCCC document portal (six official
  languages) and OECD OLIS/One; no clean public API on either (expect
  scraping or manual bulk export). Early material (INC, first COPs, 1990s
  DAC directives) is often scanned — OCR needed precisely for the pre-2007
  period the history cares about.
- **No DOIs, no reference lists** — the layer joins the text/embedding layer
  but stays outside citations.csv; disclose in the paper's bias section.
  (Recent OECD publications DO carry 10.1787 DOIs and are already in via
  OpenAlex — the dedup test below guards against re-adding them.)
- **No redistribution restriction** — derived text can ship in the deposit,
  unlike publisher-restricted academic abstracts.
- Document symbols (e.g. FCCC/CP/2009/11/Add.1, DCD/DAC(2016)3/FINAL) are
  the natural identifiers.

## Empirical basis (added 2026-07-22, author's angle)

The extension is bounded empirically, not by an a priori institution list:
cross the resubmitted Œconomia manuscript's bibliography against the v1
corpus and let the classes of absent-but-needed documents define the harvest.
Probe result (81 works cited, 31 in corpus; script and full class analysis in
`deliverables/data-paper/revision-rdj26561/biblio-vs-corpus-probe.md`):

- The only absent classes the manuscript actually needed are the official
  international-institution documents already targeted — UNFCCC decisions and
  the Biennial Assessment series (the corpus holds the 5th BA but not the
  2014 first edition, and commentary about Bali/Copenhagen/Cancún rather
  than the decisions themselves), plus GEF-type reports. The sub-lists above
  are confirmed; no open-ended new class emerges.
- Boundary decisions SETTLED by the author (2026-07-22): party/negotiating-
  bloc submissions (e.g. AOSIS on the NCQG) and negotiation-record commentary
  (IISD Earth Negotiations Bulletin summaries, statements at COPs) are
  INCLUDED — both sub-lists extend accordingly. Carbon markets and ESG stay
  DELIBERATE corpus-scope exclusions, to be stated in the paper's scope/bias
  section: MacKenzie 2009 (CDM/HFC-23 commensuration) and Golka 2024
  (ESG/impact-investing epistemics, zero climate-finance occurrence in
  abstract/keywords/intro) were verified on the documents — their absence is
  by design, not a query gap.
- Regression check: rerun the probe against corpus v2 after the harvest —
  classes A and B in the probe note should then match.

Sequencing warning: this is the largest work item of the revision and blocks
the final corpus numbers (every {{< meta >}} count refreshes). Start early;
prose tickets can draft against v1 numbers but the final render waits for
this. The `in_v1` flag preserves v1 reproducibility for the Œconomia
companion; the new corpus ships in 0280's new Zenodo version.

## Relevant files

- `scripts/catalog_*` — existing source harvesters, pattern for a new one
- Grey-literature seed mechanism (16 curated reports + World Bank API) —
  the layer extends this source, `from_grey` provenance flag
- `scripts/enrich_*` — abstract-reconstruction pathway to extend
- `config/` keyword taxonomy — finance-relevance rule for COP decisions
- Ticket 0280 — the deposit version this layer ships in

## Actions

1. Freeze the inclusion rule (proposal, amended per the 2026-07-22 decisions:
   official documents produced by or addressed to the COP or the OECD DAC
   that define, account for, or govern climate finance — including party/bloc
   submissions — plus the negotiation record around those decisions: session
   summaries such as the IISD Earth Negotiations Bulletin and statements
   delivered at COPs) and the two key-document sub-lists; author validates
   both BEFORE harvesting. Carbon markets and ESG remain outside corpus
   scope by design.
2. Build the harvester/curated importer: metadata (UN symbol, title, year,
   language, body), PDF retrieval, text extraction with OCR fallback.
3. Derive abstract-equivalents, flagged as reconstructed; embed via the
   standard multilingual pipeline.
4. Integrate as a Phase-1 source (provenance flag, dedup against existing
   grey seeds — some of the 16 are likely UNFCCC documents), rerun Phase 1
   on padme (coordinate with author; data flows padme→doudou).
5. Refresh corpus numbers and the paper's source/bias sections; update
   @tbl-sources and @tbl-quality.

## Test

- Harvester unit-tested on fixture metadata; dedup test proves an existing
  UNFCCC seed report is not duplicated by the new layer, and that an
  OECD work already present via its 10.1787 DOI is not re-added.

## Verification

- [x] Inclusion rule and document list validated by the author (2026-07-22:
      amended rule + both sub-lists at class level; the document-level list
      materializes at harvest within the validated classes)
- [x] Layer present in the regenerated corpus with correct provenance flags
      (verified 2026-07-27: `from_unfccc` 230, `from_oecd` 33,
      `abstract_provenance` populated. One exception carried to 0347:
      `keywords_provenance` is empty corpus-wide — emitter fixed here, data
      repopulation is a Phase-1 rerun on the frozen corpus, author's call)
- [x] v1 subset reproducible via `in_v1` after the rerun (98.1% of the 29,875
      v1 identifiers recovered; 503 removed by v2's stricter filter, 58
      absent from the v2 harvest — quantified, see log)

## Invariants

- Phase separation (manuscript build never triggers Phase 1); `in_v1`
  reproducibility; no hand-curated numbers in the paper.
- The layer enters as a durable pipeline source (author constraint,
  2026-07-22): `catalog_*`-style harvesters/importers, rerunnable, with
  provenance flags and tests — never a one-shot manual dump into the corpus.

## Exit criteria

- [x] UNFCCC + OECD DAC layer in the v2 corpus, counts refreshed in
      data-paper.qmd (@tbl-quality: 232/230/225 UNFCCC, 35/33/33 OECD DAC,
      matching a direct query of refined_works.csv)
- [x] Citation-network exclusion of the layer disclosed in the bias section
      (section 2.3, added here — was only generic no-DOI wording before)
- [x] Response-letter bullet drafted: R1-06 answered by extension within the
      frame + 0281's boundary rationale for national institutions
      (response-letter.md:81-101)
