%erg 0.1
Title: The corpus boundary contradicts itself: the stated definition excludes what Figure 3 shows as major clusters
Created: 2026-07-27
Author: HDMX-coding-agent
Label: needs-human

--- log ---
2026-07-27T11:00Z HDMX-coding-agent created
2026-07-27T11:00Z HDMX-coding-agent note child of 0274. Raised by 4 of 4 external panel reviewers — the single most convergent finding. Marked needs-human: the resolution is an author scope decision, not an implementation choice.
2026-07-27T13:30Z HDMX-coding-agent note AUTHOR DECISION: option (c) — own the real-world fuzziness. Describe how the boundary is drawn, acknowledge that adjacent material passes the net in quantity, and state the preference plainly: recall of climate finance over selectivity against its neighbours. Neither narrowing the corpus nor retitling.
2026-07-27T13:30Z HDMX-coding-agent note implementing that decision surfaced a factual error in section 1: the paper claimed "we filter out pure green finance", and no such filter exists. See Finding below. The boundary is drawn at the query, conjunctively, and the quality filters do the opposite of what the sentence claimed.
2026-07-27T16:40Z HDMX-coding-agent note action 4 done — cluster labels VERIFIED CORRECT, and the panel's objection REFUTED. openai/gpt-5.5 (student) reported "Kyoto mechanisms & CDM appears to grow from 2% before 2007 to 20% after 2015, which is counterintuitive". Measured from tab_sem6_assignments.csv (8,315 works): cluster 1 runs 41.4% -> 20.8% -> 2.1% across the three periods. It contracts by a factor of 20, exactly as a Kyoto-era mechanism should. The 2%/20% pair the reviewer quotes belongs to cluster 3, "Paris Agreement & loss and damage", which runs 1.3% -> 2.6% -> 18.3% — the reviewer read the right numbers off the wrong panel. No relabelling needed.

--- body ---
## Context

All four external reviewers (2026-07-27 panel) independently identified the
same defect, and openai/gpt-5.5 (grinchy) made it the lead objection behind a
Reject recommendation: **the corpus does not match its own definition.**

The Introduction defines climate finance as "North-South financial flows
directed at mitigating and adapting to climate change" and states that pure
green finance is filtered out. Ticket 0288 records the author's settled
decision that **carbon markets and ESG are deliberate corpus-scope
exclusions**. Yet Figure 3 presents "Green bonds & sustainable finance",
"Carbon markets & EU ETS", and "Renewable energy & sustainable growth" as
major thematic clusters, and Figure 3 shows the Kyoto/CDM cluster growing to
~20% of the post-2015 period.

Either the definition is wrong about the corpus, or the corpus is wrong
about the definition. As written, a reader cannot tell which — and the paper
prints both.

Reviewer framing: *"This is not a minor definitional quibble. If the
operational definition is closer to 'climate-related finance, policy
mechanisms, carbon markets, and selected sustainable-finance topics', then
the title, abstract, and Introduction overstate the specificity of the
corpus."*

**The circularity sharpener** (mistralai/mistral-large, student persona):
the four-tier keyword taxonomy was mined from 2,644 core papers already known
to be climate finance, so the temporal distribution in Figure 1 partly
measures the taxonomy rather than the field. The proposed test is a
**prospective-taxonomy ablation** — rebuild the query from period-contemporary
sources only (UNFCCC glossaries, early IPCC and UNCED reports, pre-2009
institutional vocabulary) and compare the recovered temporal profile against
the retrospective taxonomy's. If the take-off year survives, the circularity
objection is answered empirically rather than conceded in prose.

## Finding: there is no green-finance filter, and §1 said there was

Section 1 read: *"As we filter out pure green finance to retain only works
related to international climate finance…"*. Checked against the config that
actually ran:

- **Nothing filters green finance out.** There is no green-finance flag in
  `corpus_filter.yaml`. The six flags are citation isolation, cross-encoder
  relevance, absent abstract with irrelevant title, missing metadata, title
  blacklist, and semantic outlier.
- **The filters do the opposite.** `safe_title` protects works whose titles
  carry `green bond`, `green finance`, `carbon market`, `carbon tax`,
  `carbon price`, `carbon credit`, `carbon trading`, `carbon offset`, or
  `renewable` — that list exists to stop the flags from removing them.
- **The cross-encoder query is broad**: "climate policy and financial
  mechanisms". The fallback LLM prompt is broader still, naming carbon
  markets and green investment as inclusion criteria.

**What actually bounds the corpus is the query taxonomy, conjunctively.**
Every green/carbon-market term in `openalex_queries.yaml` is searched paired
with a climate term: `green bonds climate`, `sustainable finance climate`,
`carbon offset climate`, `carbon credit climate`, `emissions trading
climate`, `green investment climate`, `green economy climate`. Pure green
finance is never searched for; it enters only inside a climate-searched work
or through one of the seven non-OpenAlex sources.

That rule is clean enough to state in the paper, which is what option (c)
needs. **One exception**, disclosed in the prose rather than hidden: the
Japanese Tier-1 term グリーンファイナンス (green finance) is unconjoined.
`carbon finance` is also unconjoined, which is defensible — it denotes
climate finance, not its neighbours.

**This reconciles 0288.** Carbon markets and ESG were excluded as *seeding*
decisions — the harvest does not go looking for them — not as filter rules.
Adjacent material that arrives via climate vocabulary was always kept. The
two decisions never conflicted; the prose misdescribed one of them.

## Relevant files

- `deliverables/data-paper/data-paper.qmd` — §1 (definition), §2.1, §4, Figure 3
- `tickets/0288-datapaper-unfccc-grey-literature-layer.erg` — the recorded
  exclusion decision, log 2026-07-22T10:00Z and body lines 73–79, 108–109
- `config/` — the keyword taxonomy that actually ran
- `scripts/analysis/` — the k-means cluster labelling behind Figure 3

## Actions

1. ~~Author decision~~ — DONE, option (c), logged above.
2. ~~Correct §1's false filter claim~~ — DONE. Deleted, not replaced. The
   sentence's trailing clause ("green finance at large --- a broader field
   than this dataset covers by design") already carries the explanatory work.
3. ~~State the operational rule in §2.1~~ — DROPPED by the author. A first
   attempt added a paragraph describing the conjunctive query rule, its one
   exception, and the recall preference; the author cut it as verbose. The
   §1 clause was false and had to go; the mechanism does not need explaining
   in the paper. Net effect on the corpus: -16 words. The mechanism stays
   documented here, in the Finding above.
4. ~~Figure 3's cluster labels~~ — DONE, and the panel was wrong. Measured
   from `tab_sem6_assignments.csv` (8,315 works), share of each period:

   | Cluster | pre-2007 | 2007-2014 | post-2015 |
   |---|---|---|---|
   | 0 Renewable energy & sustainable growth | 16.3 | 19.5 | 24.7 |
   | 1 Kyoto mechanisms & CDM | **41.4** | 20.8 | **2.1** |
   | 2 Carbon markets & EU ETS | 14.2 | 19.7 | 21.2 |
   | 3 Paris Agreement & loss and damage | **1.3** | 2.6 | **18.3** |
   | 4 Green bonds & sustainable finance | 1.7 | 11.4 | 14.4 |
   | 5 Land use, forests & agriculture | 25.1 | 26.1 | 19.3 |

   Cluster 1 contracts by a factor of 20, which is exactly what a Kyoto-era
   mechanism should do. The "2% before 2007 to 20% after 2015" the reviewer
   attributes to it is cluster 3's trajectory — right numbers, wrong panel.
   Every label matches its period profile. No relabelling needed, and the
   "off-scope clusters" reading collapses with it: green bonds and carbon
   markets grow because the field did, not because the harvest leaked.

   Residue for someone else: 0332's cut left `fig_sem_composition` displayed
   by no deliverable while `paths.mk` still lists it under `DATAPAPER_FIGS`.
   Filed separately.
5. **Remaining — the prospective-taxonomy ablation, still an author call.**
   Rebuild the query from period-contemporary sources only (UNFCCC glossaries,
   early IPCC and UNCED reports, pre-2009 institutional vocabulary) and
   compare the recovered temporal profile against the retrospective
   taxonomy's. Costs a harvest pass; answers the circularity objection
   empirically instead of conceding it in prose. §2.1 already concedes it —
   the panel's point is that conceding is not quantifying.
6. **Remaining — optional config hygiene, not before resubmission.** Conjoin
   or drop the unconjoined Japanese Tier-1 green-finance term so the stated
   rule holds without an exception. This changes the harvest, so it belongs
   to a future corpus version, not to this revision.

## Test

- A guard asserting the paper's scope table and the harvest taxonomy's
  top-level categories agree — a category marked "out" must not appear as a
  tier in the config.
- If the ablation runs: the recovered take-off year under the prospective
  taxonomy, recorded as a var, with the retrospective year alongside.

## Verification

- [ ] §1's definition and Figure 3's clusters no longer contradict
- [ ] 0288's exclusion decision is either upheld in the figure or amended
- [ ] The inclusion/exclusion rule is operational (a reader could apply it)

## Invariants

- Any narrowing of the corpus invalidates every number in §2–§4 and the
  Zenodo deposit. If option (a) is chosen, this ticket blocks resubmission
  and the deadline needs renegotiating with the editor.

## Exit criteria

- [ ] The author's scope decision is recorded in this ticket's log
- [ ] The paper states one operational boundary and holds to it throughout
- [ ] The circularity objection is either quantified or explicitly bounded
