%erg 0.1
Title: Split Makefile by workpackage (analysis / per-paper writing)
Created: 2026-04-23
Author: claude

--- log ---
2026-04-23T12:06Z claude created
2026-06-18T15:30Z HDMX-coding-agent note Author reframe (during R&R §3.1 work, merged PR #815): model content/{figures,tables,_includes} explicitly as Phase 2 (analytics) deliverables / Phase 3 (writing) inputs, with proper Makefile dependency management. Concrete friction observed: rendering manuscript.qmd in a fresh worktree failed because content/{figures,tables} are gitignored (DVC-only) and absent until `make data`/`dvc pull`; worked around by hand-copying from the main checkout (a hack). Inconsistency to resolve: content/_includes IS git-tracked, but content/figures and content/tables are gitignored/DVC-only. Open design fork for actions 1/4: (a) git-commit the byte-reproducible figure/table deliverables (save_figure already makes PNGs byte-stable) so the clean-room writing build needs only TeX Live + Pandoc; or (b) keep DVC and have each writing .mk declare the artifacts as prerequisites restored via `make data`/`dvc pull`. Author leans toward first-class tracked deliverables with explicit deps; git-vs-DVC is the author call. See memory reference_trackchange_review_workflow for the worktree-render friction.
2026-06-18T15:50Z HDMX-coding-agent note Refined after a size/role inventory (PR #816) — SUPERSEDES the binary git-vs-DVC fork above. Evidence: content/figures=12MB, content/tables=44MB, content/_includes=312KB; neither figures nor tables are in dvc.yaml outs and have no .dvc pointers, so the Context's "restored via dvc pull" premise is wrong — they are gitignored and regenerated by Phase 2 only. The 44MB is 80% one file (tab_lexical_tfidf.csv, 35MB) plus tab_pole_papers.csv (3.8MB) and qa_type_report.csv (3.7MB) — analysis intermediates, not paper tables; the manuscript's real deliverables total ~316KB. Decision is a size/role split, not git-vs-DVC: git-track the small byte-stable writing-facing deliverables (like _includes already is), evict the large analysis intermediates out of content/tables to an analysis-side dir, and keep DVC scoped to corpus data only. Concrete steps added as actions 6-9 in the body.
2026-06-28T19:45Z HDMX-coding-agent note Manuscript writing workpackage DONE on branch t0131-manuscript-wp. Implements decision actions 6/7 for the manuscript: the 3 PNGs + tab_venues.md (~316 KB, byte-stable) are now git-tracked via ignore-then-negate in .gitignore; render rules moved out of Makefile into a standalone manuscript.mk + _quarto-manuscript.yml profile that excludes sibling docs from Quarto project discovery (a bare render walked all 6 docs and failed on the OTHER papers' includes). Proven clean-room: `make -f manuscript.mk output/content/manuscript.pdf` builds the PDF in a data-less worktree with only Quarto/XeLaTeX. Tracker STAYS OPEN. Remaining work (datapaper/techrep/companion/ncc .mk + profiles, action 8 eviction of tab_lexical_tfidf.csv/tab_pole_papers.csv/qa_type_report.csv) spun out to child 0163-split-build-remaining-writing-workpackag.erg.

--- body ---
## Context

The project already fans out: one analysis → manuscript, data paper,
companion, technical report, NCC variant, corpus report. Phony target
groups (`analysis-*`, `manuscript-*`, `datapaper-*`, `companion-*`,
`techrep-*`) and the sub-Makefiles (`companion.mk`, `divergence.mk`) show
the split is 80 % present; the harness `coding.md` rule just asks to
finish it so each writing-side build works with no analysis toolchain.

One wrinkle: handoffs are stored via DVC, not committed directly.
`content/figures/` and `content/tables/` are gitignored; restoration relies
on `dvc pull` against the `.dvc` files that *are* tracked. This satisfies
the intent of the rule (versioned durable state), but the clean-room build
now requires `dvc` client + remote credentials, not only TeX Live.

## Actions

1. Audit the existing phony groups to confirm the workpackage partition:
   which targets *must* live on the analysis side (Python, data access)
   and which can live on the writing side (Pandoc/LaTeX + `dvc pull`).
2. Extract `manuscript.mk`, `datapaper.mk`, `companion.mk` (promote the
   existing file), `techrep.mk`, and if warranted `ncc.mk` — each taking
   only `content/**` DVC-restored artifacts and source prose as inputs.
3. Keep analysis targets (`corpus-*`, `figures`, `stats`, `corpus-tables`,
   `corpus-handoff`, `check-*`) in the top-level `Makefile`.
4. Confirm every file that a writing-side Makefile reads is either
   committed to git, tracked by a `.dvc` file, or declared as a source
   prose input. No implicit dependencies on pipeline state.
5. Document the writing-side toolchain explicitly (TeX Live + Pandoc +
   `dvc` client + remote credentials, no `uv sync`).

## Decision — size/role split (2026-06-18, refines actions 1/4)

A per-file inventory (PR #816) shows `content/figures` (12 MB) and
`content/tables` (44 MB) are **not** DVC outputs — absent from `dvc.yaml`, no
`.dvc` pointers — so the Context's "restored via `dvc pull`" premise is
inaccurate; they are gitignored and regenerated by Phase 2 only. The 44 MB is
80 % one file (`tab_lexical_tfidf.csv`, 35 MB) plus `tab_pole_papers.csv`
(3.8 MB) and `qa_type_report.csv` (3.7 MB) — analysis intermediates, not paper
tables. The manuscript's real deliverables total ~316 KB
(`tab_venues.md` 4 KB + two PNGs). `content/tables/` therefore conflates two
roles; separate them rather than pick git-vs-DVC wholesale.

6. Classify every file in `content/{figures,tables}` as a *writing-facing
   deliverable* (small, byte-stable, `\input`/`![]()`-ed by a paper) or an
   *analysis intermediate* (large, consumed only by other Phase-2 scripts).
7. git-track the deliverables: drop their `.gitignore` entries and commit them
   (`save_figure` already makes PNGs byte-stable, so no churn), bringing figures
   and the small tables in line with the already-tracked `content/_includes/`.
8. Evict analysis intermediates (`tab_lexical_tfidf.csv`, `tab_pole_papers.csv`,
   `qa_type_report.csv`, …) out of `content/tables/` into an analysis-side
   derived/scratch dir; update the producing and consuming Make targets. DVC
   them only if they must persist across machines.
9. Keep DVC scoped to genuine corpus data (`data/catalogs/*`, embeddings). The
   clean-room writing build then needs only TeX Live + Pandoc — no `dvc` client,
   no credentials (simplifies action 5 and the Test/Exit criteria below).

## Test

For each writing workpackage: in a container with only the writing toolchain
(no `uv`, no Python analysis deps), run `dvc pull` then
`$(MAKE) -f <wp>.mk`. The PDF must build.

## Exit criteria

- One writing-side Makefile per output paper, each buildable standalone.
- Every input to a writing Makefile is either committed to git or DVC-tracked.
- CI job (or documented local recipe) demonstrates the clean-room build for
  at least the manuscript WP.
- Writing-toolchain spec in `README.md` or equivalent.
