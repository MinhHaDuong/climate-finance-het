%erg 0.1
Title: Data paper: deduplication weaknesses and per-procedure removal counts
Created: 2026-07-20
Author: HDMX-coding-agent

--- log ---
2026-07-20T09:00Z HDMX-coding-agent created
2026-07-20T10:00Z HDMX-coding-agent note split counts-extraction into 0284; this ticket is prose only
2026-07-22T14:00Z HDMX-coding-agent note 0284 artifact ready — data/catalogs/run_reports/catalog_merge__<run_id>.json (DVC-tracked). Per-procedure counts on md5-verified inputs (unified_works.csv md5 4b3c9c4bf0a54be5988017921350980f): doi_duplicates_removed=833, title_year_duplicates_removed=154, dropped_empty_title=3; records_total=43906 → records_unified=42916. DVC-commit of run_reports pending a dvc-capable session (regenerated deterministically by dvc repro catalog_merge).

2026-07-22T12:31Z HDMX-coding-agent note blocker 0284 closed — Blocked-by removed.
2026-07-23T15:45Z HDMX-coding-agent note 0301 artifact ready — deliverables/_shared/tables/tab_dedup_error_estimates.csv (committed, regenerated by make from current catalogs). Headline numbers: FN exact-title/same-author pairs (year gap<=5) 329 (1.1% of 30987 refined docs; pilot judged ~half genuine WP/published versions, so ~0.5% of corpus); fuzzy candidate-family upper bound 1064 docs (3.4%). FP: DOI pass 833 removals/797 groups, 113 divergent-title groups, 38 near-zero-overlap groups (~OpenAlex DOI collisions, ~0.12% of corpus); title+year pass 129 groups, 48 author-conflict groups (swap-aware upper bound; pilot's manual read: genuine wrong merges ~5-10), 7 empty-year-key groups merging 18 docs (max group "climate finance|" = 6) — the degenerate empty-year key is the identified weakness to footnote or fix. Local run reads 6 of 8 source catalogs (unfccc/oecd not pulled on this checkout), hence 147 vs the official 154 title+year removals.
--- body ---
## Context

RDJ-26561 R&R (tracker 0274), remarks ED-01 (dedup part), R1-12. The referee:
DOI-based dedup misses WP-vs-published pairs (different or absent DOIs);
title+year dedup misses them too (dates and sometimes titles differ). Suggests
incorporating the author variable. Asks for the number of duplicates removed
by each procedure: (1) DOI-based, (2) title+year matching.

The counts themselves are produced by ticket 0284 (pipeline artifact, possibly
padme-side); this ticket is the manuscript revision that cites them.

## Relevant files

- `deliverables/data-paper/data-paper.qmd` — Method section, dedup paragraph
- 0284's counts artifact (path recorded in that ticket on close)

## Actions

1. Add the per-procedure counts (from 0284's artifact) and an honest
   discussion of WP-vs-published residual duplicates to the paper.
2. Evaluate (possibly as future work rather than implementation) author-aware
   dedup; state the decision and its rationale in the text or response letter.

## Test

- The two counts quoted in the paper match 0284's archived artifact.

## Verification

- [ ] Counts traceable to 0284's artifact

## Invariants

- No hand-curated numbers; Phase separation (manuscript build never triggers
  Phase 1).

## Exit criteria

- [ ] data-paper.qmd states duplicates removed by DOI-based and title+year
      procedures and discusses the WP-vs-published limitation
- [ ] Response-letter bullet drafted (feeds 0283)
