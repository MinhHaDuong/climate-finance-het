%erg 0.1
Title: Collecter les deux sources non archivées des 21 places comptées du Viet Nam
Created: 2026-09-22
Author: HDMX-coding-agent

--- log ---
2026-09-22T10:22Z HDMX-coding-agent created
2026-09-22T11:05Z HDMX-coding-agent note lancé par l'auteur le 2026-09-22 sur padme
2026-09-22T11:20Z HDMX-coding-agent note collecte : 2/2 collected (HTTP 200, HTML) — eeas-jetp-project-progress-2025 6f3afc18 (60 684 o), moit-project-index-2026 ad1077b4 (21 523 o) ; aucun échec ; l'index MOIT est rendu côté serveur, les trois fiches nommées (Bác Ái, Bình Dương–Đồng Nai, Trị An) sont dans le texte archivé, prise telle quelle sans pagination ni autre route. Snapshot DVC 264 → 266 fichiers, poussé, « Cache and remote 'padme' are in sync ». Vues régénérées (VNM.json, documents.json 314 → 316 lignes, 156 656 o sous 512 000, overview.json) et recensement 0817 (unavailable → admitted) ; observations/*.json inchangés, aucune ligne de liaison ne cite ces sources. Les deux sources ne figurent pas dans la liste épinglée de test_jetp_observatory_observations.py : test de registre ajouté dans test_jetp_project_coverage.py, rouge contre le manifest d'origin/main, vert ici. Playwright depuis #documents : 2/2 ouvrent leur copie locale (href = local_path, sha256 conforme, titre lu) ; la remontée « Facts relying on it · 3 » liste les trois fiches nommées, les 21 places n'y figurent que par le compte unpublished, par conception. check-fast 2113 passed, lint 338 passed.

--- body ---

## Context

Décidé par l'auteur le 2026-09-22, en explorant le MVP après la fusion de
[[0854]]. Le Viet Nam affiche « 3 named · 21 unpublished » : quatre places
de projets initiaux et dix-sept propositions présélectionnées, annoncées par
la divulgation officielle de juillet 2025, portées par `data/jetp/projects.csv`
en `verification_status = official_count_slot` et documentées ligne par ligne
dans `data/jetp/project-coverage.csv`. Chacune de ces 21 lignes de couverture
cite le même triplet de sources : `vnm-moit-newsletter-05-2025-07` (archivé),
`vnm-eeas-jetp-project-progress-2025` (note du Service européen pour l'action
extérieure du 2025-07-09) et `vnm-moit-project-index-2026` (index des projets
sur le site du MOIT). Les deux derniers sont dans `data/jetp/sources.csv` mais
absents de `data/jetp/manifest.csv` : jamais collectés. Deux des trois sources
qui fondent le 21 ne sont donc pas archivées. Même geste que [[0854]].

Ne pas lancer avant que l'auteur le demande.

## Relevant files

- `data/jetp/sources.csv` — les deux entrées et leur URL
- `data/jetp/manifest.csv`, `data/jetp/documents/` — registre et snapshot DVC
- `scripts/jetp/corpus_harvest_documents.py`, cible `jetp-harvest` — collecteur
- `data/jetp/project-coverage.csv` — les 21 lignes qui citent ces sources
- `tests/test_jetp_observatory_observations.py` — épingle les observations sans empreinte ; vérifier si ces deux sources y comptent

## Actions

1. Sur padme (la capture DVC y est contrainte par `jetp-documents-track`),
   `make jetp-harvest JETP_SOURCE_ID=<id>` pour chacune des deux sources ;
   consigner le résultat tel qu'écrit par le collecteur, jamais une ligne
   inventée. L'index MOIT est une page de liste, possiblement paginée ou
   dynamique : un échec est un résultat.
2. `make jetp-documents-track`, `dvc push`, commit du registre et du pointeur.
3. Régénérer `documents.json` et les vues touchées ; le pre-commit reste à
   512 000 octets.
4. Rejouer le pas qui compte : depuis la page Documents, chaque source
   collectée ouvre en local, et sa remontée liste ce qui la cite.

## Test

Red : le test qui épingle les lignes sans empreinte
(`tests/test_jetp_observatory_observations.py`) ou, si ces sources n'y
figurent pas, un test de registre : les deux `source_id` ont une tentative
de collecte consignée, `collected` ou un statut d'échec nommé.

## Verification

- [x] Registre : une ligne par source, statut consigné — `data/jetp/manifest.csv` lignes 316–317, statut `collected` (commit f7d9854e)
- [x] Page Documents : les sources collectées ouvrent en local — Playwright 2/2, href = local_path, sha256 conforme (log 2026-09-22T11:20Z)
- [x] `dvc status -c` en phase après push — « Cache and remote 'padme' are in sync » après `dvc push` (3 files pushed)

## Exit criteria

- [x] Les deux sources ont une tentative de collecte dans le registre, avec son résultat — deux lignes `collected` dans `data/jetp/manifest.csv` (commit f7d9854e) ; `tests/test_jetp_project_coverage.py::test_the_vietnamese_count_slot_sources_have_a_recorded_collection_attempt` (commit 94ddde89)
- [x] Les vues servies sont régénérées sur ce registre — VNM.json, documents.json, overview.json (commit 2089ee24) ; recensement 0817 (commit 71074ed0)
- [x] `make check-fast` et `make lint` passent — sur 94ddde89 : check-fast 2113 passed, 15 skipped ; lint 338 passed, 12 skipped (log 2026-09-22T11:20Z)
