%erg 0.1
Title: Le MVP s'organise selon O, D, E, M sans en afficher les mots ; D se lit comme un parcours
Created: 2026-09-23
Author: HDMX-coding-agent

--- log ---
2026-09-23T06:45Z HDMX-coding-agent created

2026-09-23T12:17Z HDMX-coding-agent note cold read by the author (2026-09-23), decision: the Glossary moves to the end of the navigation, beside How we did this, grouped by theme and A–Z within each group; every address becomes its label's slug (#funding, #on-the-record, #by-the-numbers, #how-we-did-this, #historical-comparison, #release-history, #entries/<CODE>, #on-the-record/<CODE>), and the earlier addresses forward to them by replaceState. Test, Invariants and Exit reworded accordingly (PR #1459).
2026-09-23T12:28Z HDMX-coding-agent note second batch of the author's cold read (2026-09-23), decision: the page top becomes two bars. The header holds the four sections as tabs (The paper trail, By the numbers, Glossary, How we did this). On paper-trail pages a step bar (Documents › Entries › On the record › Projects · Funding · Who's who) marks the current step with aria-current and shows a removable country chip. It replaces the eyebrow, the trail block and the in-page Entries / On the record tabs, which become two steps (#entries/<CODE>, #on-the-record/<CODE>; ?tab=record still forwards). The breadcrumb appears on detail pages only, and the title block is an h1, a one-sentence lede and a folded 'About this page'. Markers are paired: 'Our calculation' on computed figures and 'As published' on publishers' figures. This Test section's 'l'indicateur de son étape et des liens vers l'étape voisine' is met by the step bar: the marked step is the indicator, and the neighbouring steps are links in the same bar (PR #1459).
2026-09-23T13:40Z HDMX-coding-agent note third batch of the author's cold read (2026-09-23), with the author's revision of item 7: the Who's who fold-out reads its count once, and the Glossary's group headings are set at sub-heading size. The section first called By the numbers is now 'The tallies' at #the-tallies; #by-the-numbers and #numbers forward to it. Its page is one table: a row per computed figure (what it is, value, unit, what it covers, as of, computed from), grouped by country and never summed across countries, followed by the two charts as numbered figures, each marked 'Our calculation'. It no longer shows the stat grid or the card panels; the landing page keeps its stat grid under the eyebrow 'The tallies' (PR #1459).
--- body ---

## Context

Décisions de l'auteur du 2026-09-23 (`docs/jetp-ontology.md` décision 11,
`docs/jetp-language.md`, `docs/jetp-observatory-presentation.md`). Les quatre objets ODEM sont le principe d'organisation du MVP :
ce qui est groupé ensemble, dans quel ordre, et ce qui renvoie à quoi. Ils
n'en sont pas le vocabulaire. La présentation suppose un lecteur qui sait
intuitivement comment procède un travail empirique, et ne lui jette pas au
visage « Ontology », « Evidence » ou « Model » ; les lettres O, D, E, M et
les codes D1 à D4 restent dans le code, les attributs de données et les
documents de conception.

Aujourd'hui le MVP parle de trois « étages » (0834), mêle comptes (E) et
fiches (D4) sous « faits réconciliés », et emploie « evidence » pour le
soutien documentaire.

## Relevant files

- `deliverables/jetp-observatory/app.js`, `index.html`, `styles.css` — navigation, pages, libellés
- `deliverables/jetp-observatory/README.md` — « What is included »
- `docs/jetp-language.md` (objets, étapes D1 à D4, termes retirés)
- `docs/jetp-observatory-presentation.md` (organisation et vocabulaire des pages)
- `tests/test_jetp_observatory_render.py`, `tests/_jetp_observatory_render.js`, `tests/browser/jetp_observatory.py`

## Actions

1. Réorganiser la navigation selon les objets, avec le vocabulaire de
   rédaction décidé le 2026-09-23 (`docs/jetp-observatory-presentation.md`),
   dans l'ordre fixé par la lecture à froid de l'auteur du 2026-09-23 :
   **The paper trail** (D) : **Documents** → **Entries** → **On the record** →
   **Projects**, **Funding**, **Who's who** ; **The tallies** (E) ;
   **Glossary** (O), groupé par thème et A–Z dans chaque groupe ;
   **How we did this** (méthodes). Aucune entrée pour M. Chaque élément
   « On the record » se lit « according to » son éditeur, avec la date.
   Chaque adresse est le slug de son libellé (`#funding`, `#on-the-record`,
   `#the-tallies`, `#how-we-did-this`, …) ; les anciennes adresses
   redirigent vers les nouvelles.
2. D se lit comme une piste documentaire : chaque page montre sa place sur
   la piste et permet de monter ou descendre d'une étape, d'un projet à ce
   qui est « on the record » à son sujet, aux entrées, à la page du
   document, et retour.
3. Tout nombre calculé par le ledger se distingue visuellement d'un nombre
   imprimé par un éditeur : il porte son unité, son périmètre et ouvre les
   déclarations dont il est calculé.
4. Balayage de la copie publique et du README : ni les noms du cadre (O, D,
   E, M, ontology, evidence, model, D1 à D4) ni les termes retirés de `docs/jetp-language.md`
   (layer, stage, reconciled, edition au sens de publication du ledger), ni
   les mots écartés par `docs/jetp-observatory-presentation.md` (claims, deals, players, sources, entities,
   records). How we did this dit en mots simples ce que l'observatoire ne
   fait pas.
5. Coordination : `app.js` est aussi touché par [[0873]] et [[0876]] ; ce
   ticket ne change aucune vue JSON servie, seulement la présentation.

## Test

Red : `tests/test_jetp_observatory_render.py` — la navigation rend, dans cet
ordre, The paper trail (Documents, Entries, On the record, Projects,
Funding, Who's who), The tallies, Glossary et How we did this, chaque
lien à l'adresse du slug de son libellé ; chaque ancienne adresse
(`#countries`, `#country/<CODE>`, `#evidence`, `#numbers`, `#comparison`,
`#methods`, `#editions`, `#inventory/<CODE>` avec `?row=` et `?tab=record`)
redirige vers la nouvelle ; une page
de la piste porte l'indicateur de son étape et des liens vers l'étape
voisine ; un élément « On the record » porte « according to » et son éditeur ;
un compte de la page Viet Nam est marqué comme calculé, avec son unité ; aucun
texte rendu ne contient « ontology », « evidence », « model », « layer »,
« reconciled » ni un code D1 à D4 (insensible à la casse, hors noms propres
de documents).

## Verification

- [ ] Recettes ZAF et VNM de [[0834]] vertes dans le navigateur, sur la nouvelle organisation
- [ ] Lecture à froid par une personne qui n'a pas lu ces documents : elle dit, sans aide, où sont les définitions, d'où vient un chiffre et quel chiffre est calculé
- [ ] Aucune vue JSON sous `deliverables/jetp-observatory/data/` ne change d'octets

## Invariants

- Les anciennes routes publiques continuent de fonctionner : elles
  redirigent (`history.replaceState`, sans rechargement) vers l'adresse du
  libellé ; le site n'émet que les nouvelles.
- Aucune addition entre lignes de documents différents ni entre lignes et référents.

## Exit criteria

- [ ] La navigation suit D, E puis O (Glossary en fin, près de How we did this) sans en afficher les noms ; aucune place pour M ; chaque adresse est le slug de son libellé et les anciennes redirigent
- [ ] La piste Documents → Entries → On the record → Projects / Funding / Who's who se parcourt dans les deux sens
- [ ] Les nombres calculés se distinguent des nombres publiés, avec unité et périmètre
- [ ] La copie publique ne contient ni les noms du cadre ni les termes retirés
